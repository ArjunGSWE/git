Just a demo repository to learn git and GitHub

git init
git add
git commit -m "<message>"
git remote add origin <ssh repository>
git push -u origin master


to add files to .gitignore just type filename
for directory type dir_name/
